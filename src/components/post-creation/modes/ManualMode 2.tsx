interface ManualModeProps {
  // No specific props needed for the simple manual mode info section
}

export const ManualMode: React.FC<ManualModeProps> = () => {
  return (
    <div className="space-y-3">
      {/* Description removed - now shown in consistent position above mode tabs */}
    </div>
  )
}